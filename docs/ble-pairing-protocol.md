<!-- SPDX-License-Identifier: MIT
  Copyright (c) 2026 sonde contributors -->
# BLE Pairing Protocol Specification

> **Document status:** Draft — under review
> **Scope:** Wire-level protocol for BLE-mediated pairing between a mobile phone app, sonde nodes, and the sonde gateway.
> **Audience:** Implementers building the mobile pairing app, the node firmware BLE handler, or the gateway pairing subsystem.
> **Related:** [pairing-protocol.md](pairing-protocol.md) (legacy USB), [protocol.md](protocol.md), [security.md](security.md)

---

## 1  Overview

A mobile phone acts as a pairing agent.  It communicates with the gateway and node over BLE to provision key material and register nodes.  The protocol has two phases:

1. **Phone–gateway pairing** (one-time) — the phone obtains the gateway's public key and receives a phone PSK.
2. **Node pairing** (per node) — the phone provisions a node PSK over BLE, and the node relays an encrypted registration payload to the gateway over ESP-NOW.

### 1.1  Actors

| Actor | Transport | Role |
|-------|-----------|------|
| Phone | BLE (to gateway and node) | Generates node PSKs, authenticates pairing requests with its phone PSK, encrypts payloads with gateway public key. |
| Node | BLE (from phone), ESP-NOW (to gateway) | Stores node PSK, relays opaque encrypted payload to gateway. |
| Gateway | BLE (to phone), ESP-NOW (from node) | Issues phone PSKs, decrypts and validates pairing requests, registers nodes. |

### 1.2  Cryptographic primitives

| Primitive | Parameters | Usage |
|-----------|------------|-------|
| HMAC-SHA256 | 32-byte key, 32-byte output | Phone authenticates pairing requests.  Node authenticates ESP-NOW frames. |
| Ed25519 | 32-byte public key, 64-byte signature | Gateway keypair (public key distributed for X25519 conversion). |
| X25519 | 32-byte keys | ECDH key agreement (ephemeral ↔ gateway). |
| HKDF-SHA256 | Variable-length IKM, salt, info | Derives AES key from ECDH shared secret. |
| AES-256-GCM | 32-byte key, 12-byte nonce, 16-byte tag | Encrypts payloads for the gateway. |
| SHA-256 | 32-byte output | key_hint derivation. |

The node only uses HMAC-SHA256 and SHA-256 (already implemented).  Ed25519, X25519, HKDF-SHA256, and AES-256-GCM are new cryptographic dependencies required on the phone and gateway only.

### 1.3  Notation

- All multi-byte integers are **big-endian** unless stated otherwise.
- `‖` denotes byte concatenation.
- `[N]` after a field name means exactly N bytes.
- `HMAC(key, data)` means HMAC-SHA256.
- `SHA256(data)` means SHA-256.

---

## 2  Key material

### 2.1  Gateway Ed25519 keypair

Generated on first gateway startup.  Private key stored encrypted at rest (GW-0601a master key).

| Item | Size | Description |
|------|------|-------------|
| `gw_private_key` | 32 bytes | Ed25519 seed (the 32-byte private keying material; never leaves the gateway). |
| `gw_public_key` | 32 bytes | Distributed to phones during Phase 1. |
| `gateway_id` | 16 bytes | Random identifier, generated alongside the keypair.  Used as HKDF salt and AES-GCM AAD. |

The Ed25519 public key is converted to X25519 using the standard birational map (RFC 8032 → RFC 7748) whenever ECDH is needed.  Implementations MUST use a conversion routine equivalent to libsodium's `crypto_sign_ed25519_pk_to_curve25519` (public key) and `crypto_sign_ed25519_sk_to_curve25519` (private key), which:

1. Decode the Ed25519 public key's compressed-Y coordinate, clear the sign bit (bit 255), and compute the corresponding X25519 u-coordinate via the birational map `u = (1 + y) / (1 - y) mod p`.
2. For the private key, hash the 32-byte Ed25519 seed with SHA-512 and clamp the lower 32 bytes per RFC 7748 §5 (clear bits 0–2, set bit 254, clear bit 255).
3. Reject the conversion if the resulting X25519 public key is a low-order point (all-zero or one of the small-order points on Curve25519), returning an error rather than proceeding with ECDH.

### 2.2  Phone PSK

Each authorized phone receives a unique 256-bit PSK from the gateway.

| Item | Size | Derivation |
|------|------|------------|
| `phone_psk` | 32 bytes | Generated by gateway from OS CSPRNG. |
| `phone_key_hint` | 2 bytes | `SHA256(phone_psk)[30..32]` — big-endian u16. |

The gateway stores each phone PSK with: `phone_key_hint`, label (UTF-8, max 64 bytes), issued-at timestamp (u64 seconds), and status (active / revoked).

### 2.3  Node PSK

Generated by the phone for each node.

| Item | Size | Derivation |
|------|------|------------|
| `node_psk` | 32 bytes | Generated by phone from OS CSPRNG. |
| `node_key_hint` | 2 bytes | `SHA256(node_psk)[30..32]` — big-endian u16. |

The key_hint derivation (`SHA256(PSK)[30..32]`) is a provisioning convention shared with the USB pairing protocol.  The radio protocol treats key_hint as an opaque 16-bit lookup optimization — the derivation method is not enforced at the protocol layer.

---

## 3  BLE GATT services

### 3.1  Service UUIDs

Two GATT services are defined — one on the gateway (or its BLE-connected modem), one on the node.

| Service | UUID | Advertised by |
|---------|------|---------------|
| Gateway Pairing Service | `0000FE60-0000-1000-8000-00805F9B34FB` | Gateway |
| Node Provisioning Service | `0000FE50-0000-1000-8000-00805F9B34FB` | Node (in pairing mode) |

> **Note:** When the gateway uses the ESP32-S3 modem for BLE, the Gateway Pairing Service is hosted on the modem but BLE advertising is **OFF by default** after boot.  The gateway (via admin CLI or API) must send `BLE_ENABLE` to the modem before a phone can discover it.  See [modem-protocol.md §4.13](modem-protocol.md) for details.

### 3.2  Gateway Pairing Service characteristics

| Characteristic | UUID | Properties | Description |
|----------------|------|------------|-------------|
| Gateway Command | `0000FE61-0000-1000-8000-00805F9B34FB` | Write, Indicate | Phone writes commands, gateway sends responses as indications. |

### 3.3  Node Provisioning Service characteristics

| Characteristic | UUID | Properties | Description |
|----------------|------|------------|-------------|
| Node Command | `0000FE51-0000-1000-8000-00805F9B34FB` | Write, Indicate | Phone writes provision data, node sends ACK as indication. |

> **Note:** UUIDs are provisional placeholders using the Bluetooth SIG base UUID.  These 16-bit short UUIDs in the `0xFE**` range are reserved by the SIG and MUST NOT ship without assignment.  Before v1.0, these will be replaced with randomly-generated vendor-specific 128-bit UUIDs (not based on the SIG base UUID) to avoid collisions with assigned services.

### 3.4  MTU and fragmentation

Both services require an ATT MTU ≥ 247 bytes (negotiated via MTU exchange).  Messages larger than (MTU − 3) bytes use **Write Long** (ATT Prepare Write + Execute Write) for writes.  For indications (gateway/node → phone), the sender fragments the complete envelope into chunks of at most (MTU − 3) bytes and sends each chunk as a separate indication.  The receiver reassembles by buffering incoming indication payloads in order until the accumulated length matches the `LEN` field from the envelope header (§4).  Messages MUST NOT be interleaved — the sender MUST complete all indications for one message before starting the next.  Each indication MUST be acknowledged (ATT Handle Value Confirmation) by the receiver before the sender transmits the next chunk.

If MTU negotiation yields < 247, the phone should disconnect and report an error.

---

## 4  BLE message envelope

All messages on both the Gateway Command and Node Command characteristics use a common envelope:

```
┌──────────┬──────────┬───────────────────────────┐
│ TYPE (1B)│ LEN (2B) │ BODY (0..65535 bytes)      │
│          │ BE u16   │                             │
└──────────┴──────────┴───────────────────────────┘
```

| Field | Size | Description |
|-------|------|-------------|
| `TYPE` | 1 byte | Message type discriminator (§4.1, §4.2). |
| `LEN` | 2 bytes, big-endian u16 | Length of BODY in bytes. |
| `BODY` | 0–65535 bytes | Type-specific payload (up to `LEN` bytes). |

Total envelope overhead: 3 bytes.

### 4.1  Phone–gateway message types (Gateway Command characteristic)

| Type | Direction | Name | Description |
|------|-----------|------|-------------|
| 0x01 | Phone → GW | `REQUEST_GW_INFO` | Request gateway public key. |
| 0x02 | Phone → GW | `REGISTER_PHONE` | Register phone as pairing agent. |
| 0x81 | GW → Phone | `GW_INFO_RESPONSE` | Gateway public key + id. |
| 0x82 | GW → Phone | `PHONE_REGISTERED` | Encrypted phone PSK + channel. |
| 0xFF | Either | `ERROR` | Error response. |

#### 4.1.1  ERROR body layout

| Offset | Size | Field   | Description          |
|--------|------|---------|----------------------|
| 0      | 1    | status  | Error code (u8)      |
| 1      | var  | message | UTF-8 diagnostic text |

**Status codes:**

| Code | Meaning |
|------|---------|
| 0x01 | Generic error |
| 0x02 | Registration window closed |
| 0x03 | Already paired |

### 4.2  Phone–node message types (Node Command characteristic)

| Type | Direction | Name | Description |
|------|-----------|------|-------------|
| 0x01 | Phone → Node | `NODE_PROVISION` | Provision PSK + encrypted payload. |
| 0x81 | Node → Phone | `NODE_ACK` | Provision acknowledgement. |
| 0xFF | Either | `ERROR` | Error response. |

---

## 5  Phase 1 — Phone-to-gateway pairing

### 5.1  Sequence

```
Phone                              Gateway
  │                                   │
  │── [BLE connect] ─────────────────►│
  │── [MTU exchange, ≥247] ──────────►│
  │── [LESC Just Works pairing] ─────►│
  │                                   │
  │── REQUEST_GW_INFO ───────────────►│  (write to Gateway Command)
  │            ◄── GW_INFO_RESPONSE ──│  (indication: pubkey + id + signed challenge)
  │                                   │
  │  (phone verifies gateway signature│
  │   over the challenge nonce)       │
  │                                   │
  │── REGISTER_PHONE ────────────────►│  (write to Gateway Command)
  │                                   │
  │  (gateway checks registration     │
  │   window is open, generates       │
  │   phone PSK)                      │
  │                                   │
  │            ◄── PHONE_REGISTERED ──│  (indication, encrypted)
  │                                   │
  │── [BLE disconnect] ─────────────►│
```

### 5.2  REQUEST_GW_INFO (0x01)

**Body: 32 bytes.**

```
Offset  Size  Field
─────── ───── ────────────────
0       32    challenge           Random nonce generated by the phone
```

The phone generates a 32-byte random challenge nonce from OS CSPRNG.  The gateway must sign this nonce to prove it holds the Ed25519 private key corresponding to the public key it advertises.

> **Trust-on-first-use (TOFU):**  On the phone's first connection to a gateway, there is no pre-existing trust anchor — an attacker could present their own keypair and produce a valid signature.  The phone MUST persist `gw_public_key` and `gateway_id` after first successful verification (pin-on-first-use).  Subsequent connections reject any gateway presenting a different public key.  For environments requiring stronger assurance, the operator SHOULD verify the gateway's public key fingerprint out-of-band (e.g., displayed on the gateway's admin interface and confirmed visually on the phone).

### 5.3  GW_INFO_RESPONSE (0x81)

**Body: 112 bytes.**

```
Offset  Size  Field
─────── ───── ────────────────
0       32    gw_public_key       Ed25519 public key
32      16    gateway_id          Random gateway identifier
48      64    signature           Ed25519 signature over (challenge ‖ gateway_id)
```

The gateway signs the concatenation of the phone's challenge nonce (from `REQUEST_GW_INFO`) and its own `gateway_id`.  Including `gateway_id` in the signed payload binds the signature to this specific gateway, preventing an attacker from replaying a signature obtained from a different gateway.

**Phone verification:**

1. Verify `signature` over `(challenge ‖ gateway_id)` using `gw_public_key`.
2. If verification fails → disconnect, report error ("gateway authentication failed").
3. If verification succeeds → the phone has confirmed the gateway holds the private key.  Persist `gw_public_key` and `gateway_id`.

### 5.4  REGISTER_PHONE (0x02)

**Body: 33 + label_len bytes.**

```
Offset  Size        Field
─────── ─────────── ──────────────────────
0       32          ephemeral_pubkey    Phone's ephemeral X25519 public key
32      1           label_len           Length of label (0–64)
33      label_len   label               UTF-8 phone label
```

The phone generates a fresh ephemeral X25519 keypair for this request.  The ephemeral public key enables the gateway to encrypt the response.

#### 5.4.1  Registration window

The gateway MUST reject `REGISTER_PHONE` requests unless a **registration window** is currently open.  The registration window is opened by an explicit operator action:

- **Physical button** on the gateway (or modem) held for ≥2 seconds, OR
- **Admin API call** (planned `OpenPhoneRegistration` gRPC — not yet defined in `admin.proto`; will be added in a future PR).

The registration window remains open for a configurable duration (default: 120 seconds) and closes automatically.  While closed, `REGISTER_PHONE` receives an `ERROR` response (type `0xFF`) with status `0x02` ("Registration window closed", per §4.1.1).

When the registration window is opened, the gateway also sends `BLE_ENABLE` to the modem to start BLE advertising.  When the window closes, the gateway sends `BLE_DISABLE` to stop advertising.  See [modem-protocol.md §4.13](modem-protocol.md) and GW-1222.

This prevents an unauthorized device from silently registering as a pairing agent by simply connecting to the gateway's BLE service.

### 5.5  PHONE_REGISTERED (0x82)

**Body: 12 + ciphertext_len bytes.**

The gateway encrypts the response so the phone PSK is never sent in plaintext (even within the BLE LESC session):

1. Convert `gw_private_key` (Ed25519 seed) to X25519 private key: the Ed25519 private key is stored as a 32-byte seed (the raw keying material from which the expanded secret key is derived).  To obtain the X25519 private key, compute `SHA-512(seed)` and take the first 32 bytes, then clamp per RFC 7748 §5 (clear bits 0, 1, 2 of the first byte; clear bit 7 and set bit 6 of the last byte).  This matches the procedure used by libsodium's `crypto_sign_ed25519_sk_to_curve25519`.  Implementations using an "expanded" 64-byte Ed25519 secret key (seed ‖ public) MUST extract the first 32 bytes (the seed) before hashing.
2. `shared_secret` = X25519(gw_x25519_private, ephemeral_pubkey from REGISTER_PHONE).
3. `aes_key` = HKDF-SHA256(ikm = shared_secret, salt = gateway_id, info = `"sonde-phone-reg-v1"`, len = 32).
4. `nonce` = 12 random bytes from OS CSPRNG.
5. `ciphertext ‖ tag` = AES-256-GCM-Encrypt(key = aes_key, nonce = nonce, plaintext = inner, aad = gateway_id).

Wire format:

```
Offset  Size             Field
─────── ──────────────── ──────────────────────
0       12               nonce           AES-256-GCM nonce
12      ciphertext_len   ciphertext      Encrypted inner + 16-byte GCM tag
```

The **inner plaintext** (36 bytes, only meaningful when `status = 0x00`):

```
Offset  Size  Field
─────── ───── ────────────────
0       1     status          0x00 = accepted
1       32    phone_psk       256-bit phone PSK
33      2     phone_key_hint  BE u16
35      1     rf_channel      WiFi channel (1–13)
```

Registration rejection is signaled exclusively via `ERROR` (0xFF) with the appropriate status code (§4.1.1) — `PHONE_REGISTERED` is only sent on success (`status = 0x00`).

### 5.6  Error handling

| Condition | Behavior |
|-----------|----------|
| MTU < 247 | Phone disconnects, reports error. |
| `GW_INFO_RESPONSE` not received within 5 s | Phone disconnects, reports timeout. |
| Gateway signature verification fails | Phone disconnects, reports error ("gateway authentication failed — possible impersonation"). |
| `PHONE_REGISTERED` not received within 30 s | Phone disconnects, reports timeout (allows time for operator confirmation). |
| `ERROR` (0xFF) with status `0x02` received | Phone reports rejection ("registration window not open — ask operator to enable pairing"), disconnects. |
| `ERROR` (0xFF) with status `0x03` received | Phone reports rejection ("already paired with this gateway"), disconnects. |
| `ERROR` (0xFF) with other status received | Phone reports generic rejection, disconnects. |
| AES-GCM decryption failure | Phone reports error (ephemeral key mismatch), disconnects. |

### 5.7  Phone persistence

On successful registration, the phone persists:

| Item | Source |
|------|--------|
| `gw_public_key` | From `GW_INFO_RESPONSE` |
| `gateway_id` | From `GW_INFO_RESPONSE` |
| `phone_psk` | From decrypted `PHONE_REGISTERED` |
| `phone_key_hint` | From decrypted `PHONE_REGISTERED` |
| `rf_channel` | From decrypted `PHONE_REGISTERED` |

---

## 6  Phase 2 — Node pairing

### 6.1  Prerequisites

The phone has completed Phase 1 and holds: `gw_public_key`, `gateway_id`, `phone_psk`, `phone_key_hint`, `rf_channel`.

### 6.2  User input

| Field | Required | Constraints |
|-------|----------|-------------|
| `node_id` | Yes | UTF-8, 1–64 bytes.  Human-readable identifier. |
| `sensors` | No | CBOR array of sensor descriptors (schema in §6.3.1). |

### 6.3  Pairing request construction

The phone builds a **PairingRequest** as a CBOR map with integer keys:

```cbor
{
  1: node_id,           ; tstr (1–64 bytes)
  2: node_key_hint,     ; uint (u16, 0–65535)
  3: node_psk,          ; bstr (32 bytes)
  4: rf_channel,        ; uint (1–13)
  5: sensors,           ; array (optional, see §6.3.1)
  6: timestamp          ; uint (Unix seconds)
}
```

CBOR encoding MUST use deterministic encoding (RFC 8949 §4.2): integer keys in ascending order, minimal-length integer encoding, definite-length containers.

#### 6.3.1  Sensor descriptor schema

Each sensor is a CBOR map:

```cbor
{
  1: sensor_type,       ; uint (enum: 1=i2c, 2=adc, 3=gpio, 4=spi)
  2: sensor_id,         ; uint (0–255, bus address or pin number)
  3: label              ; tstr (optional, max 64 bytes UTF-8, human-readable name)
}
```

Example: `[{1: 1, 2: 0x48, 3: "soil_moisture"}, {1: 2, 2: 3, 3: "battery"}]`

### 6.4  Authentication and encryption

Step 1 — **Authenticate** with phone PSK:

```
hmac_input    = PairingRequest_cbor_bytes
phone_hmac    = HMAC-SHA256(phone_psk, hmac_input)

authenticated_request =
    phone_key_hint[2]  ‖  PairingRequest_cbor_bytes  ‖  phone_hmac[32]
```

Total: 2 + len(cbor) + 32 bytes.

Step 2 — **Encrypt** with gateway public key:

1. Generate ephemeral X25519 keypair: `(eph_private, eph_public)`.
2. Convert `gw_public_key` (Ed25519) to X25519 public key.
3. `shared_secret` = X25519(eph_private, gw_x25519_public).
4. `aes_key` = HKDF-SHA256(ikm = shared_secret, salt = gateway_id, info = `"sonde-node-pair-v1"`, len = 32).
5. `nonce` = 12 random bytes from OS CSPRNG.
6. `ciphertext ‖ tag` = AES-256-GCM-Encrypt(key = aes_key, nonce = nonce, plaintext = authenticated_request, aad = gateway_id).

**Encrypted pairing payload:**

```
Offset  Size              Field
─────── ───────────────── ──────────────────
0       32                eph_public          Ephemeral X25519 public key
32      12                nonce               AES-256-GCM nonce
44      variable          ciphertext          Encrypted authenticated_request + 16B GCM tag
```

Total: 44 + len(authenticated_request) + 16 bytes.

### 6.5  Sequence

```
Phone                    Node                    Gateway
  │                        │                        │
  │── [BLE connect] ──────►│                        │
  │── [MTU exchange] ─────►│                        │
  │── [LESC Just Works] ──►│                        │
  │                        │                        │
  │── NODE_PROVISION ─────►│                        │
  │         ◄── NODE_ACK ──│                        │
  │                        │                        │
  │── [BLE disconnect] ───►│                        │
  │                        │── (reboot) ────────────│
  │                        │── PEER_REQUEST ───────►│  (ESP-NOW)
  │                        │◄── PEER_ACK ───────────│  (ESP-NOW)
```

### 6.6  NODE_PROVISION (0x01, Phone → Node)

**Body: 37 + payload_len bytes.**

```
Offset      Size          Field
─────────── ───────────── ──────────────────────────
0           2             node_key_hint     BE u16
2           32            node_psk          256-bit PSK
34          1             rf_channel        WiFi channel (1–13)
35          2             payload_len       BE u16, length of encrypted_payload
37          payload_len   encrypted_payload Opaque blob for gateway (§6.4)
```

The node stores all fields in NVS.  The `encrypted_payload` is opaque to the node.

### 6.7  NODE_ACK (0x81, Node → Phone)

**Body: 1 byte.**

```
Offset  Size  Field
─────── ───── ──────────────
0       1     status
```

| Status | Meaning |
|--------|---------|
| 0x00 | Stored successfully. |
| 0x01 | Already paired (PSK exists, no pairing button override). |
| 0x02 | Storage error (NVS write failed). |

### 6.8  Error handling

| Condition | Behavior |
|-----------|----------|
| MTU < 247 | Phone disconnects, reports error. |
| `NODE_ACK` not received within 5 s | Phone disconnects, reports timeout. |
| `NODE_ACK` status ≠ 0x00 | Phone reports error, disconnects. |
| Node receives `NODE_PROVISION` while already paired (no button override) | Node sends `NODE_ACK(0x01)`. |

---

## 7  ESP-NOW transport (Node ↔ Gateway)

### 7.1  PEER_REQUEST (msg_type 0x05, Node → Gateway)

Uses the standard ESP-NOW frame format from [protocol.md](protocol.md):

```
┌─────────────────────────────────────────┬───────────────┬────────┐
│ Header (11 bytes)                       │ CBOR payload  │ HMAC   │
│ key_hint[2] ‖ msg_type[1] ‖ nonce[8]   │               │ [32]   │
└─────────────────────────────────────────┴───────────────┴────────┘
```

| Header field | Value |
|-------------|-------|
| `key_hint` | Node's key_hint (2 bytes, BE u16). |
| `msg_type` | `0x05`. |
| `nonce` | 8-byte random nonce (generated by node per transmission; **not** a sequence number — see note below). |

> **Nonce semantics:**  `PEER_REQUEST`/`PEER_ACK` use a **random** nonce, not the gateway-assigned sequence number used by post-WAKE messages (`COMMAND`, `DATA`, etc.).  These messages are sent before any session is established, so no sequence counter exists.  Gateway implementations MUST NOT apply the normal post-WAKE sequence-number anti-replay checks to `msg_type` `0x05`/`0x84` frames.  See [protocol.md](protocol.md) §3.1 for the standard nonce semantics.
>
> **Note:** These message types (`0x05`/`0x84`) extend the base protocol defined in `protocol.md`.  `protocol.md` will be updated to include them in a follow-up PR.

**CBOR payload** (map with integer keys):

```cbor
{ 1: bstr }    ; key 1 = encrypted_payload (opaque blob from phone)
```

CBOR integer keys are scoped per `msg_type` — key `1` in a `PEER_REQUEST` is unrelated to key `1` in a `WAKE` message.  This follows the same convention as the existing protocol ([protocol.md](protocol.md)).

**HMAC**: HMAC-SHA256 over (header ‖ cbor_payload), keyed with the node PSK.

The node transmits `PEER_REQUEST` on each boot cycle until it receives `PEER_ACK(0x00)`.  Retransmission interval follows the normal wake cycle schedule (default 60 seconds).

### 7.2  PEER_ACK (msg_type 0x84, Gateway → Node)

```
┌─────────────────────────────────────────┬───────────────┬────────┐
│ Header (11 bytes)                       │ CBOR payload  │ HMAC   │
│ key_hint[2] ‖ msg_type[1] ‖ nonce[8]   │               │ [32]   │
└─────────────────────────────────────────┴───────────────┴────────┘
```

| Header field | Value |
|-------------|-------|
| `key_hint` | Node's key_hint (echoed back). |
| `msg_type` | `0x84`. |
| `nonce` | Echoes the `nonce` from the corresponding PEER_REQUEST (same as the nonce-echo pattern used in COMMAND). |

**CBOR payload**:

```cbor
{
  1: uint,    ; status (0 = registered)
  2: bstr     ; registration_proof (32 bytes)
}
```

The only status value sent is `0` (registered).  All error conditions result in silent discard — no `PEER_ACK` is sent.  This is consistent with the radio protocol's silent-discard security model ([protocol.md §8](protocol.md#8--error-handling)).

The `registration_proof` field is `HMAC-SHA256(node_psk, "sonde-peer-ack-v1" ‖ encrypted_payload)` — an HMAC binding the acknowledgement to the exact encrypted payload.  Both the node and the gateway can compute this value (both have the node PSK and the encrypted_payload bytes).

An active BLE MITM attacker who defeats Just Works pairing can observe `NODE_PROVISION` and capture both `node_psk` and `encrypted_payload`.  (Passive eavesdropping alone cannot recover GATT writes when LESC encryption is active.)  This constitutes a **node PSK compromise** — the attacker can race the legitimate node to register first, or impersonate the node on ESP-NOW after registration.  The `node_id` uniqueness check (step 10 in §7.3) and timestamp tolerance (±86400 s, step 9) limit replay and duplication but do not prevent a race.  The primary mitigation is using a **MITM-resistant BLE pairing method** (Passkey Entry or Numeric Comparison) to prevent interception.  Just Works is acceptable only in low-threat environments where physical proximity provides adequate assurance.

**Node verification of PEER_ACK:**

1. Verify frame HMAC over (header ‖ cbor_payload) using node PSK.
2. Verify `nonce` matches the nonce sent in the PEER_REQUEST.
3. Compute expected `registration_proof` = `HMAC-SHA256(node_psk, "sonde-peer-ack-v1" ‖ encrypted_payload)` using the stored encrypted payload from NVS.
4. If `registration_proof` does not match → discard (potential forgery).
5. If all checks pass → set registration-complete flag.
6. **The node retains the encrypted payload in NVS** until its first successful WAKE/COMMAND exchange with the gateway.  Only after the gateway responds to a normal WAKE message (proving the registration was actually accepted) does the node erase the encrypted payload.  This ensures that even if a forged PEER_ACK is accepted, the node can recover by retransmitting the PEER_REQUEST if the subsequent WAKE fails.

**HMAC**: HMAC-SHA256 over (header ‖ cbor_payload), keyed with the node PSK.

### 7.3  Gateway processing of PEER_REQUEST

The gateway performs these steps **in order**.  Failure at any step causes **silent discard** — no `PEER_ACK` is sent.  This is consistent with the radio protocol's error handling ([protocol.md §8](protocol.md#8--error-handling)):

1. **Parse header.** Extract key_hint, verify msg_type = 0x05.  If not → discard.
2. **Bypass normal key-hint lookup.**  Because the node is not yet registered, the `key_hint` may be unknown **or may collide** with an existing node's key_hint (key_hint is a 16-bit non-unique value).  For `msg_type = 0x05` (PEER_REQUEST), the gateway MUST NOT attempt the normal radio fast-path (key-hint lookup → HMAC verification).  Instead, skip directly to CBOR parsing and decryption — the frame HMAC is verified later (step 8) using the `node_psk` extracted from the decrypted payload.
3. **Parse CBOR.** Extract `encrypted_payload` (key 1, bstr).  If missing or malformed → discard.
4. **Decrypt.**
   - Extract `eph_public[32] ‖ nonce[12] ‖ ciphertext[...]` from encrypted_payload.
   - Convert `gw_private_key` (Ed25519 seed) to X25519 private key (see §5.5 step 1).
   - `shared_secret` = X25519(gw_x25519_private, eph_public).
   - `aes_key` = HKDF-SHA256(ikm = shared_secret, salt = gateway_id, info = `"sonde-node-pair-v1"`, len = 32).
   - Decrypt: `authenticated_request` = AES-256-GCM-Decrypt(key = aes_key, nonce = nonce, ciphertext = ciphertext, aad = gateway_id).
   - If decryption fails (bad GCM tag) → discard.
5. **Parse authenticated_request.**
   - `phone_key_hint` = bytes[0..2] (BE u16).
   - `cbor_bytes` = bytes[2 .. len−32].
   - `phone_hmac` = bytes[len−32 .. len].
6. **Verify phone HMAC.**
   - Look up all phone PSKs matching `phone_key_hint` in the phone trust store (key_hint is a 16-bit non-unique lookup hint; collisions are possible).
   - For each matching PSK that is not revoked, compute expected = HMAC-SHA256(phone_psk, cbor_bytes).
   - If `phone_hmac` matches any candidate → accept that PSK.
   - If no candidate matches (or all are revoked) → discard.
7. **Parse PairingRequest CBOR.**  Extract node_id, node_key_hint, node_psk, rf_channel, sensors, timestamp.
8. **Verify frame HMAC.**
   - Using the extracted `node_psk`, compute HMAC over (header ‖ cbor_payload) from the original ESP-NOW frame.
   - If HMAC mismatch → discard.
9. **Verify timestamp.** If `|now − timestamp| > 86400` → discard.
10. **Check node_id uniqueness.** If node_id already registered → discard.
11. **Verify key_hint consistency.** Frame header key_hint must match `node_key_hint` in the PairingRequest.  If mismatch → discard.
12. **Register node.** Store (node_id, node_key_hint, node_psk, rf_channel, sensors, registered_by = phone_id) where `phone_id` is the gateway's internal identifier for the phone PSK record (e.g., database primary key or a stable fingerprint of the phone PSK).  The `phone_key_hint` is a 16-bit non-unique lookup hint and MUST NOT be used as the sole audit identifier — collisions would misattribute registrations.
13. **Send PEER_ACK(0).** Compute `registration_proof` = `HMAC-SHA256(node_psk, "sonde-peer-ack-v1" ‖ encrypted_payload)`.  Build CBOR payload `{ 1: 0, 2: registration_proof }`.  HMAC the frame with the node PSK.  The `nonce` field echoes the `nonce` from the PEER_REQUEST header.

### 7.4  HMAC bootstrap rationale

The PEER_REQUEST frame is HMAC'd with the node PSK (step 8), but the gateway doesn't know the node PSK until after decrypting the inner payload (step 4–7).  This is safe because:

- The encrypted payload can only be decrypted by the gateway (ECDH confidentiality).
- The phone HMAC proves the PSK was issued by an authorized phone (authentication).
- The frame HMAC proves the transmitting node holds the same PSK (proof of provisioning).
- An attacker who intercepts the node PSK over BLE cannot forge a *new* encrypted payload for a different `node_id` (requires the phone PSK for HMAC authentication and the gateway public key for ECDH encryption).  However, the attacker *can* replay the captured `encrypted_payload` and `node_psk` together to craft a valid `PEER_REQUEST` for the same node identity.  This replay/race risk is mitigated by the `node_id` uniqueness check at the gateway, the PairingRequest timestamp tolerance (±86400 s), and — for high-assurance deployments — using a MITM-resistant BLE pairing method (Passkey Entry or Numeric Comparison) to prevent interception in the first place.

---

## 8  Node firmware behavior

### 8.1  Boot priority

On reset, the node checks conditions in this order:

| Priority | Condition | Action |
|----------|-----------|--------|
| 1 | USB-CDC detected | Enter legacy USB pairing mode ([pairing-protocol.md](pairing-protocol.md)). |
| 2 | No PSK in NVS **or** pairing button held (GPIO LOW for ≥500ms after reset) | Enter BLE pairing mode (§8.2). |
| 3 | PSK stored, registration-complete flag NOT set | Send PEER_REQUEST over ESP-NOW (§8.3). |
| 4 | PSK stored, registration-complete flag set | Normal WAKE cycle. |

### 8.2  BLE pairing mode

1. Start BLE stack, register Node Provisioning Service (§3.3).
2. Begin advertising with service UUID `0000FE50-0000-1000-8000-00805F9B34FB` (Node Provisioning Service, §3.1/§3.3).  Advertising name: `"sonde-XXXX"` where `XXXX` is the last 4 hex digits of the MAC address.
3. On BLE connection: negotiate MTU ≥ 247, accept LESC Just Works pairing.
4. On `NODE_PROVISION` write:
   a. Parse fields per §6.6.
   b. If already paired and pairing button was NOT held → respond `NODE_ACK(0x01)`.
   c. If pairing button was held → erase existing PSK first (factory reset), then proceed.
   d. Write to NVS: `node_psk`, `node_key_hint`, `rf_channel`, `encrypted_payload`.
   e. Clear registration-complete flag.
   f. Respond `NODE_ACK(0x00)`.
5. Remain in BLE pairing mode (allows re-provision on error).
6. On BLE disconnect → reboot.

### 8.2.1  Factory reset via BLE

There is no dedicated BLE factory-reset message.  To factory-reset a node via BLE:

1. Hold the pairing button during boot to enter BLE pairing mode.
2. Send a new `NODE_PROVISION` — step 4c above erases the existing PSK and all persistent state before writing the new credentials.

For explicit factory reset without re-pairing, use the USB `RESET_REQUEST` command ([pairing-protocol.md](pairing-protocol.md) §4.3).

### 8.3  Post-provision boot (registration pending)

1. Initialize ESP-NOW on the stored `rf_channel`.
2. Load `encrypted_payload` from NVS.
3. Build and transmit `PEER_REQUEST` frame (§7.1).
4. Listen for `PEER_ACK` for 10 seconds.
5. On `PEER_ACK(0)` with valid HMAC, nonce echo, and registration_proof:
   - Set registration-complete flag in NVS.
   - Retain `encrypted_payload` in NVS (erased after first successful WAKE — see §8.3.1).
   - Proceed to normal WAKE cycle.
6. On timeout, HMAC failure, or proof mismatch:
   - Enter deep sleep.
   - Retry on next wake (default 60 seconds).

#### 8.3.1  Deferred payload erasure

After the registration-complete flag is set, the node attempts a normal WAKE cycle.  If the gateway responds with a valid COMMAND (proving the node is registered and the gateway knows its PSK), the node erases the `encrypted_payload` from NVS.

If the WAKE fails (no response or HMAC failure), the node clears the registration-complete flag and reverts to sending `PEER_REQUEST` on the next boot.  This self-healing behavior ensures that a forged `PEER_ACK` (from an attacker who intercepted the node PSK) does not permanently brick the node.

### 8.4  NVS layout

| NVS key | Type | Size | Description |
|---------|------|------|-------------|
| `magic` | u32 | 4 | Presence marker (`0x534F4E44` — big-endian ASCII "SOND"). |
| `key_hint` | u32 | 4 | Node key_hint (stored as u32, used as u16). |
| `psk` | blob | 32 | Node PSK. |
| `channel` | u32 | 4 | RF channel (1–13). |
| `peer_payload` | blob | variable | Encrypted pairing payload.  Retained until the first successful WAKE/COMMAND exchange, then erased. |
| `reg_complete` | u32 | 4 | Registration-complete flag (1 = registered). |
| `interval` | u32 | 4 | Wake cycle interval (seconds). |
| `active_p` | u32 | 4 | Active program partition (0 or 1). |
| `prog_a` | blob | ≤4096 | Program partition A. |
| `prog_b` | blob | ≤4096 | Program partition B. |

---

## 9  Security analysis

### 9.1  Threat model summary

| Threat | Mitigated by |
|--------|-------------|
| Rogue gateway impersonation (Phase 1) | Gateway signs phone's challenge nonce with Ed25519 private key; phone verifies before proceeding (§5.3). |
| Unauthorized phone registration | Registration window must be opened by operator action; gateway rejects `REGISTER_PHONE` when closed (§5.4.1). |
| Rogue phone registers a node | Phone HMAC verification (step 6 in §7.3).  Attacker needs a valid phone PSK. |
| Compromised phone PSK | Gateway operator revokes the phone PSK.  Previously registered nodes remain valid. |
| BLE eavesdrop / MITM captures node PSK | `NODE_PROVISION` sends both `node_psk` and `encrypted_payload` over BLE.  Passive eavesdropping cannot recover this data when LESC encryption is active; an **active MITM** (defeating Just Works) is required.  Mitigated by one-time-use of the encrypted payload (`node_id` uniqueness, step 10 in §7.3), timestamp tolerance (±86400 s, step 9), and using Numeric Comparison (default) to prevent MITM. |
| BLE active MITM captures node PSK | Same as above.  Numeric Comparison (the default pairing method) prevents active MITM by requiring operator verification of a 6-digit passkey.  Just Works is acceptable as a fallback for low-threat environments without operator presence. |
| Forged PEER_ACK | `registration_proof` = HMAC over encrypted payload (§7.2) raises the bar.  Deferred payload erasure (§8.3.1) ensures the node self-heals if a forged ACK is accepted — it reverts to PEER_REQUEST if the subsequent WAKE fails. |
| Replay of PEER_REQUEST | Timestamp check (±24h) + node_id uniqueness (step 9–10 in §7.3). |
| Phone A reads Phone B's pairing requests | Impossible — each encrypted with gateway public key, authenticated with different phone PSK. |
| Node impersonation after registration | HMAC-SHA256 on every ESP-NOW frame (existing radio protocol). |

### 9.2  BLE link security

BLE LESC Numeric Comparison is the **default** pairing method for the **gateway pairing service** (phone ↔ modem/gateway).  The modem relays the 6-digit passkey to the gateway via `BLE_PAIRING_CONFIRM` (modem-protocol.md §4.15), and the gateway displays it to the operator via the admin CLI.  The operator verifies that the pin matches the phone's display and confirms via `BLE_PAIRING_CONFIRM_REPLY`.  This provides MITM protection for the BLE link.

> **Note:** The node provisioning service (phone ↔ node) uses LESC Just Works, because nodes are headless devices with no operator interface to display or confirm a pin.  Numeric Comparison applies only to the gateway/modem side.

Just Works remains available as a fallback for the gateway pairing service in environments without operator presence, but does not provide MITM protection.  When using Just Works, an active MITM can defeat the pairing and capture both the `node_psk` and `encrypted_payload` from `NODE_PROVISION`, which is sufficient to craft a valid `PEER_REQUEST`.  The primary mitigations are the one-time-use nature of the encrypted payload (the gateway rejects duplicate `node_id` registrations, step 10 in §7.3) and the PairingRequest timestamp tolerance (±86400 s, step 9 in §7.3).  The 120 s registration window applies only to Phase 1 phone registration (`REGISTER_PHONE` §5.4.1), not to Phase 2 node registration.

For even higher assurance, BLE Passkey Entry can be used in place of Numeric Comparison — the protocol is agnostic to the specific LESC pairing method.

---

## 10  Timing

| Event | Timeout | Action on timeout |
|-------|---------|-------------------|
| GW_INFO_RESPONSE after REQUEST_GW_INFO | 5 s | Phone disconnects, reports error. |
| PHONE_REGISTERED after REGISTER_PHONE | 30 s | Phone disconnects, reports error (allows operator confirmation). |
| NODE_ACK after NODE_PROVISION | 5 s | Phone disconnects, reports error. |
| PEER_ACK after PEER_REQUEST | 10 s | Node enters deep sleep, retries next wake. |

---

## 11  Constants

| Constant | Value | Description |
|----------|-------|-------------|
| Gateway Pairing Service UUID | `0000FE60-0000-1000-8000-00805F9B34FB` | BLE service on gateway. |
| Node Provisioning Service UUID | `0000FE50-0000-1000-8000-00805F9B34FB` | BLE service on node. |
| Gateway Command char UUID | `0000FE61-0000-1000-8000-00805F9B34FB` | Write + Indicate. |
| Node Command char UUID | `0000FE51-0000-1000-8000-00805F9B34FB` | Write + Indicate. |
| Minimum ATT MTU | 247 bytes | Required for both services. |
| `PEER_REQUEST` ESP-NOW msg_type | 0x05 | Node → Gateway. |
| `PEER_ACK` ESP-NOW msg_type | 0x84 | Gateway → Node. |
| HKDF info (phone registration) | `"sonde-phone-reg-v1"` | UTF-8 bytes, no null terminator. |
| HKDF info (node pairing) | `"sonde-node-pair-v1"` | UTF-8 bytes, no null terminator. |
| HKDF salt | `gateway_id` (16 bytes) | Same for both operations. |
| Ed25519 public key | 32 bytes | |
| X25519 public key | 32 bytes | |
| AES-256-GCM nonce | 12 bytes | |
| AES-256-GCM tag | 16 bytes | |
| HMAC-SHA256 output | 32 bytes | |
| PSK (phone and node) | 32 bytes | 256-bit. |
| key_hint derivation | `SHA256(psk)[30..32]` | Lower 16 bits, big-endian u16. |
| Timestamp tolerance | ±86400 s | 24 hours. |
| PEER_ACK registration proof info | `"sonde-peer-ack-v1"` | UTF-8 bytes, no null terminator. Used in `registration_proof` HMAC. |
| Registration window duration | 120 s | Default; configurable by operator. |
| Pairing button GPIO | TBD | Active LOW, sampled for 500ms after reset. |
| Node BLE advertising name | `"sonde-XXXX"` | XXXX = last 4 hex digits of BLE MAC. |
| Max node_id length | 64 bytes | UTF-8. |
| Max sensor label length | 64 bytes | UTF-8. |
| Max encrypted_payload size | 202 bytes | Must fit in ESP-NOW frame (250B max), see §11.1. |

### 11.1  Encrypted payload size budget

The ESP-NOW maximum frame payload is 250 bytes.  The PEER_REQUEST frame budget:

```
Header:            11 bytes  (key_hint[2] + msg_type[1] + nonce[8])
CBOR overhead:      4 bytes  (1-pair map: 0xA1 + key + bstr-2 header = 4 bytes for payloads ≤ 255B)
HMAC:              32 bytes
─────────────────────────────
Available for encrypted_payload:  203 bytes (exact)
```

The constant `MAX_ENCRYPTED_PAYLOAD` is set to **202 bytes** (conservative, reserving 1 byte for possible future CBOR keys).  The `protocol.md` payload budget of 207 bytes (250 − 11 − 32) is 4 bytes larger because it does not include CBOR framing overhead.

The encrypted payload contains:

```
eph_public:        32 bytes
nonce:             12 bytes
GCM tag:           16 bytes
phone_key_hint:     2 bytes
phone_hmac:        32 bytes
CBOR PairingRequest: variable
─────────────────────────────
Fixed overhead:    94 bytes
Available for CBOR: ~108 bytes
```

A minimal PairingRequest (20-char node_id, no sensors) is ~80 bytes CBOR.  This fits.  Large sensor arrays may require splitting across multiple PEER_REQUEST frames (future extension) or limiting sensor metadata.

---

## 12  Modem relay transport

When the gateway does not have a local BLE radio, BLE GATT services are hosted on the ESP32-S3 modem and relayed to the gateway over the USB-CDC serial protocol defined in [modem-protocol.md](modem-protocol.md).

### 12.1  Architecture

```
Phone ←(BLE)→ Modem ←(USB-CDC)→ Gateway
```

The modem hosts the Gateway Pairing Service (§3.2) and relays messages opaquely:

- **Phone → Gateway:** The phone writes a BLE envelope (§4) to the Gateway Command characteristic. The modem forwards the raw bytes to the gateway as a `BLE_RECV` serial message (modem-protocol.md §4.10).
- **Gateway → Phone:** The gateway sends a `BLE_INDICATE` serial message (modem-protocol.md §4.9) containing the BLE envelope. The modem delivers it as a GATT indication on the Gateway Command characteristic, handling fragmentation per §3.4.
- **Connection events:** The modem sends `BLE_CONNECTED` (modem-protocol.md §4.11) and `BLE_DISCONNECTED` (modem-protocol.md §4.12) so the gateway can manage pairing session state.

### 12.2  Transparency

The modem does not inspect, validate, or modify BLE envelope contents.  All cryptographic operations (Ed25519 signatures, ECDH key agreement, AES-GCM encryption) are performed by the gateway.  The modem is a transparent BLE-to-serial bridge for pairing messages, analogous to its role as a transparent ESP-NOW-to-serial bridge for radio frames.

### 12.3  Concurrency

BLE pairing relay operates concurrently with ESP-NOW frame relay.  The modem MUST NOT block ESP-NOW operations while a BLE client is connected.  Both radio subsystems (BLE and ESP-NOW) run independently on the ESP32-S3.

### 12.4  BLE advertising control and Numeric Comparison relay

BLE advertising is OFF by default after modem boot/RESET to prevent continuous BLE advertising from disrupting ESP-NOW traffic.  The gateway controls advertising via `BLE_ENABLE` (modem-protocol.md §4.13) and `BLE_DISABLE` (modem-protocol.md §4.14).

The typical flow is:

1. The operator runs `sonde-admin pairing start`, which calls the `OpenBlePairing` admin API RPC.
2. The gateway opens the registration window and sends `BLE_ENABLE` to the modem.
3. The modem starts BLE advertising.  A phone can now discover and connect.
4. During LESC Numeric Comparison pairing, the modem sends `BLE_PAIRING_CONFIRM` (modem-protocol.md §4.15) with the 6-digit passkey to the gateway.
5. The gateway (via admin CLI) displays the passkey to the operator.  The operator verifies it matches the phone's display.
6. The gateway sends `BLE_PAIRING_CONFIRM_REPLY` (modem-protocol.md §4.16) to accept or reject the pairing.
7. After the pairing window closes (auto-close or `sonde-admin pairing stop`), the gateway sends `BLE_DISABLE` to the modem.

This ensures BLE and ESP-NOW do not interfere with each other during normal sensor data collection, and that BLE pairing is an explicit operator-initiated action with MITM protection via Numeric Comparison.

---

## 13  Future extensions

| Extension | Approach |
|-----------|----------|
| Phone PSK revocation list | Gateway pushes revocation updates to phones via BLE management service. |
| Multi-frame PEER_REQUEST | Add a `fragment_index` / `fragment_total` field to the CBOR payload for payloads exceeding 202 bytes. |
| Bidirectional phone ↔ gateway management | Add GATT characteristics for querying registered nodes, revoking phones, etc. |
| Firmware OTA via BLE | Separate BLE service with sequential block writes + SHA-256 verification. |
| QR code bootstrap | Node prints QR code containing its BLE MAC + a one-time bootstrap token, scanned by phone to initiate pairing. |
